# cast to-dec

将一个基数的数转换为十进制数

```bash
$ cast to-dec --help
用法： cast to-dec [OPTIONS] [VALUE]

参数：
  [VALUE]  The value to convert

选项：
  -i, --base-in <BASE_IN>  The input base
  -h, --help               Print help
```